import { AppPlaceholder } from "@/components/layout/AppPlaceholder";

export default function ClientsPage() {
  return <AppPlaceholder title="Клиенты" description="База клиентов, история посещений и заметки будут доступны в этом разделе." />;
}
